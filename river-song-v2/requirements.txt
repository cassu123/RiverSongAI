# =============================================================================
# River Song AI -- Python Dependencies
# Phase 1: Core conversation loop
# Phase 2: Google Services + Intent Router
# Phase 5: Information Feeds (Weather, News, Stocks, Sports)
# =============================================================================
# Install with: pip install -r requirements.txt
# Tested on Python 3.11+
# =============================================================================


# -----------------------------------------------------------------------------
# Web framework
# -----------------------------------------------------------------------------
fastapi==0.136.0
uvicorn[standard]==0.32.1        # ASGI server with websocket support
websockets==13.1                 # WebSocket protocol support


# -----------------------------------------------------------------------------
# Configuration
# -----------------------------------------------------------------------------
pydantic==2.9.2                  # Data validation
pydantic-settings==2.6.1         # BaseSettings for .env loading
python-dotenv==1.0.1             # .env file support


# -----------------------------------------------------------------------------
# STT -- Whisper (local)
# -----------------------------------------------------------------------------
openai-whisper==20240930         # Local Whisper inference
sounddevice==0.5.1               # Cross-platform audio I/O
numpy==1.26.4                    # Audio buffer handling (whisper dep)
scipy==1.14.1                    # Audio resampling utilities


# -----------------------------------------------------------------------------
# LLM -- Ollama client
# -----------------------------------------------------------------------------
ollama==0.3.3                    # Official Ollama Python client


# -----------------------------------------------------------------------------
# TTS -- Piper (local)
# Uses the Piper binary via subprocess -- no Python package required.
# Install Piper separately: https://github.com/rhasspy/piper
# Voice models: https://huggingface.co/rhasspy/piper-voices
# -----------------------------------------------------------------------------
soundfile==0.12.1                # Read synthesized WAV output for playback


# -----------------------------------------------------------------------------
# Kill switch
# -----------------------------------------------------------------------------
bcrypt==4.2.1                    # Password hashing for kill switch reset


# -----------------------------------------------------------------------------
# Google Services (Phase 2)
# -----------------------------------------------------------------------------
google-api-python-client==2.151.0  # Calendar, Gmail, Drive REST clients
google-auth==2.36.0                # Google OAuth 2.0 core library
google-auth-oauthlib==1.2.1        # InstalledAppFlow for desktop OAuth
google-auth-httplib2==0.2.0        # httplib2 transport for google-auth
googlemaps==4.10.0                 # Google Maps Platform Python client


# -----------------------------------------------------------------------------
# YouTube Music (Phase 2)
# -----------------------------------------------------------------------------
ytmusicapi==1.8.2                  # Unofficial YouTube Music API client
yt-dlp==2026.3.17                  # Audio stream extraction for playback


# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
httpx==0.27.2                    # Async HTTP client (used by health checks)
python-multipart==0.0.26         # Form data parsing for FastAPI
