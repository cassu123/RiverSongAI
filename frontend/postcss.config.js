export default {
  plugins: {
    // Tailwind v4 ships its PostCSS plugin separately; it also handles
    // vendor prefixing, so autoprefixer is no longer needed here.
    '@tailwindcss/postcss': {},
  },
}
