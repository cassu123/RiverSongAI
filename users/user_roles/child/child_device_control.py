# /user_roles/child/child_device_control.py

def lock_device(device_id):
    # Simulate locking the child's device
    print(f"Device {device_id} has been locked.")

def restrict_apps(device_id, apps):
    # Simulate restricting apps on the child's device
    print(f"Apps {apps} have been restricted on device {device_id}.")

def set_screen_time_limit(device_id, limit):
    # Simulate setting screen time limit on the child's device
    print(f"Screen time limit of {limit} hours set on device {device_id}.")
