# order_processor.py

def process_order(user, order):
    """
    Process the order from Walmart for the given user.
    """
    print(f"Processing order {order['id']} for user {user['name']}")
    # Logic to process the order
    # Update order status and notify the user
