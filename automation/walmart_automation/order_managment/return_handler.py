# return_handler.py

def handle_return(user, return_request):
    """
    Handle product returns for the given user.
    """
    print(f"Handling return for user {user['name']} and product {return_request['product_id']}")
    # Logic to process the return
    # Notify the user of the return status
